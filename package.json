{
  "name": "valentine_app",
  "version": "1.0.0",
  "description": "A 3-level Valentine-themed Mario-inspired platformer web app.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist/data && cp data/trivia-questions.json dist/data/trivia-questions.json",
    "preview": "vite preview"
  },
  "private": true,
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
